# port
server.port = 9100
server.servlet.context-path = /api/v1

# Database Connection
spring.datasource.url = jdbc:mysql://localhost:3306/training_management
spring.datasource.username = root
spring.datasource.password =
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# Hibernate JPA
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true

# JMS (Java Mail Sender)
spring.mail.host = smtp.gmail.com
spring.mail.port = 587
spring.mail.username = metrodataacedemy@gmail.com
spring.mail.password = thns lwye vqyd rvel
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.starttls.enable = true

# Spring Security
# spring.security.user.name = budi
# spring.security.user.password = budi
